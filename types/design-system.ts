/**
 * Shared TypeScript types for the design system.
 */

/** Button size variants */
export type ButtonSize = "sm" | "md" | "lg" | "xl";

/** Button style variants */
export type ButtonVariant =
  | "primary"
  | "secondary"
  | "ghost"
  | "accent-outline"
  | "surface";

/** Badge style variants */
export type BadgeVariant = "accent" | "neutral" | "success";

/** Text color helpers */
export type TextColor = "primary" | "secondary" | "muted" | "accent";

/** Section padding sizes */
export type SectionSize = "sm" | "md" | "lg";

/** Container widths */
export type ContainerWidth = "narrow" | "site" | "wide" | "full";
